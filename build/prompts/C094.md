# C094 — passenger-ios-shell

## Identity
You are building **passenger-ios-shell** for the MageRide platform.
Read `CLAUDE.md` and `apps/passenger-ios/CLAUDE.md` before starting.
Wave 4b · depends on: C076 (passenger-android-shell) · est. 3 session(s).

## Spec Anchors (read these files)
- `specs/D2_mageride_ui_spec.md#section-c-platform-differences`
- `specs/D2_mageride_ui_spec.md#0-3-map-marker-patterns`
- `specs/D6_mageride_integration.md#5-websocket-signalr-fan-out`
- `specs/D7_mageride_devops.md#6-kmp-mobile-build-pipeline`
- `specs/wireframes/passenger_ios.html` — the team-approved wireframe baseline for this component

## Scope
The Passenger iOS application shell — the SwiftUI parity of C076.
ADD: [R-06, D-30, MAP-01..MAP-10].

**Fences — do not cross these:**
- Parity-fenced to C076. Platform deltas are limited to D2 Section C.
- Generate here, compile and verify on macOS.

## Screens (baseline = wireframes)
- None. This component is the application shell for `specs/wireframes/passenger_ios.html`;
  it owns no wireframe screen ID. Screens belong to the sibling screen-group components.

## Deliverables
- apps/passenger-ios Xcode project consuming the KMP XCFramework via SPM
- Color asset catalog + Dynamic Type type scale from D2 §0.2; SF Symbols per the vehicle tokens
- Localizable.strings for si / ta / en
- NavigationStack + drawer-equivalent presentation, APNs deep-link routing
- MapLibre GL Native iOS + PMTiles with matching marker/cluster/polyline patterns
- SignalR Swift client with geocell join/leave, reconnect < 5 s and state recovery
- App Attest + Keychain wiring

## Definition of Done
- the app builds and launches on a simulator (verified on macOS)
- the map joins exactly 19 cells and re-subscribes with hysteresis
- all copy resolves in si/ta/en with Dynamic Type respected

## Verify
```
runs on macOS — xcodebuild -project apps/passenger-ios/PassengerApp.xcodeproj -scheme PassengerApp -destination 'platform=iOS Simulator,name=iPhone 15' test
```

## Handoff
When complete, append to `build/progress.md`:
- Component: C094 passenger-ios-shell
- Status: DONE | PARTIAL (explain)
- Notes: [any spec gaps found, decisions made]
