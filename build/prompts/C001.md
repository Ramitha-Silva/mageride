# C001 — repo-scaffold

## Identity
You are building **repo-scaffold** for the MageRide platform.
Read `CLAUDE.md` before starting.
Wave 0 · depends on: none · est. 1 session(s).

## Spec Anchors (read these files)
- `specs/architecture-design-document.md#18-2-mobile-architecture-kmp-native-ui`
- `specs/D7_mageride_devops.md#1-build-system`
- `specs/D7_mageride_devops.md#2-2-dockerfile-template`
- `CLAUDE.md`

## Scope
Build the repository skeleton every later component plugs into: the .NET solution, the root
Gradle build (KMP shared + 2 Android app modules), the npm workspace for the portals, and the
shared tool/version pinning. Add the scoped CLAUDE.md files that do not exist yet
(backend, apps/*, portals/*, shared/kmp, infra) following the two that already exist.

**Fences — do not cross these:**
- No service code, no DDL, no Docker Compose, no CI workflow — those are C002-C010.
- Do not create iOS Xcode projects here; C085/C094 own them (this host cannot build them).

## Deliverables
- backend/MageRide.sln + Directory.Build.props (net10.0, nullable enable, TreatWarningsAsErrors) + global.json (SDK 10)
- settings.gradle.kts + gradle/libs.versions.toml + gradle wrapper at repo root, including :shared and the two Android app modules
- portals/package.json npm workspace root (Node 24 LTS) with admin / fleet / web-passenger / tailwind-preset members
- .editorconfig, .gitattributes, extended .gitignore
- scoped CLAUDE.md for apps/passenger-android, apps/driver-ios, apps/passenger-ios, portals/fleet, portals/web-passenger, infra
- README.md — repo map + "which component builds what" pointer to build/manifest.yaml

## Definition of Done
- dotnet build succeeds on an empty solution with the shared props applied
- ./gradlew projects lists :shared, :apps:driver-android, :apps:passenger-android
- npm ci succeeds at the portals workspace root
- every top-level source directory has a CLAUDE.md naming its stack and verify command

## Verify
```
dotnet build backend/MageRide.sln -c Release && ./gradlew projects && npm --prefix portals ci
```

## Handoff
When complete, append to `build/progress.md`:
- Component: C001 repo-scaffold
- Status: DONE | PARTIAL (explain)
- Notes: [any spec gaps found, decisions made]
