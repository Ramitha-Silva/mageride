# C021 — ws-registry-minimal

## Identity
You are building **ws-registry-minimal** for the MageRide platform.
Read `CLAUDE.md` and `backend/CLAUDE.md` before starting.
Wave 2 · depends on: C020 (ws-iam-minimal) · est. 1 session(s).
**Milestone: walking-skeleton** — see `build/manifest.yaml` › `meta.milestones`.

## Spec Anchors (read these files)
- `specs/D3_mageride_api_contracts.md#registry-svc-post-v1-vehicles`
- `specs/D3_mageride_api_contracts.md#registry-svc-route-table`
- `specs/server_db_schema.md#2-registry`

## Scope
Walking-skeleton slice 2 of 6. The minimum vehicle identity the dispatcher and ride flow need:
create a Mode C vehicle, mark it APPROVED through a seed/dev path, list a driver's vehicles, and
select the single live publisher. ADD: [AL-09, D-03, D-37].

**Fences — do not cross these:**
- No document upload, no OCR, no onboarding step machine, no expiry job — that is C029.
- Skip the Verification-Officer queue entirely; approval here is a dev seed path.

## Deliverables
- backend/src/Registry.Api — POST /v1/vehicles, GET /v1/vehicles/mine, POST /v1/vehicles/{id}/select-live, dev-only approve endpoint
- registry.vehicles + registry.driver_profiles repositories (Dapper)
- seed script creating one approved Mode C three-wheeler for the skeleton driver
- backend/src/Registry.Api.Tests

## Definition of Done
- a driver has exactly one selectable approved Mode C vehicle after seeding
- vehicle_type values are the canonical AL-09 set; "car" is rejected
- a duplicate registration number in the active set is rejected (D-37)

## Verify
```
dotnet test backend/src/Registry.Api.Tests -c Release
```

## Handoff
When complete, append to `build/progress.md`:
- Component: C021 ws-registry-minimal
- Status: DONE | PARTIAL (explain)
- Notes: [any spec gaps found, decisions made]
