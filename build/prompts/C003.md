# C003 — db-schema-identity-registry

## Identity
You are building **db-schema-identity-registry** for the MageRide platform.
Read `CLAUDE.md` and `backend/CLAUDE.md` before starting.
Wave 0 · depends on: C001 (repo-scaffold) · est. 2 session(s).

## Spec Anchors (read these files)
- `specs/server_db_schema.md#0-conventions`
- `specs/server_db_schema.md#1-iam`
- `specs/server_db_schema.md#2-registry`
- `specs/server_db_schema.md#3-prov`
- `specs/D4_mageride_data_model.md#17b-config-launch-operating-cities`
- `specs/architecture-design-document.md#9-1-postgresql-bounded-context-schemas`

## Scope
Stand up the migration runner, then the identity and registry half of the schema.
Extensions + all schema creation; iam.*, config.operating_cities, registry.*, prov.*, and the
shared set_updated_at trigger. ADD: [AL-06, AL-08, AL-09, AL-10, AL-13, AL-14, AL-26, AL-27,
AL-29, AL-30, AL-49, AL-50, D-03, D-37, E-03, T-02, T-03, T-08].

**Fences — do not cross these:**
- Migrations are versioned .sql scripts run by DbUp/Grate. Never `dotnet ef`.
- server_db_schema.md §0.1 omits `config`, `subscription` and `transit` from CREATE SCHEMA — create all three here (see the spec-gap note in build/progress.md) so C005 can land its tables.
- Do not create trips/rides/dispatch (C004), business schemas (C005) or telemetry (C006).

## Deliverables
- backend/src/MageRide.Migrations — DbUp runner console app + one-shot `migrate` container entrypoint
- db/migrations/0001__extensions_and_schemas.sql (postgis, timescaledb, pgcrypto, citext + all 22 schemas)
- db/migrations/0002__shared_triggers.sql
- db/migrations/01xx__iam_*.sql, 02xx__config_*.sql, 03xx__registry_*.sql, 04xx__prov_*.sql
- infra/scripts/migrate-verify.sh — throwaway timescaledb-ha:pg16 container, apply twice, assert objects

## Definition of Done
- all migrations apply cleanly on an empty DB and are idempotent on a second run
- iam.sessions carries the unique partial index on (user_id, app) WHERE revoked_at IS NULL (AL-08)
- registry.vehicles has the canonical vehicle_type CHECK (AL-09) and the reg-no unique partial index (D-37)
- registry.documents allows a NULL driver_id with a fleet_id and CHECKs exactly one owner (AL-50)
- registry.onboarding_steps + registry.document_fields exist with the AL-29/AL-30 status domains
- every temporal column is TIMESTAMPTZ; business-date columns carry the Asia/Colombo tz_at audit field (D-38)

## Verify
```
bash infra/scripts/migrate-verify.sh
```

## Handoff
When complete, append to `build/progress.md`:
- Component: C003 db-schema-identity-registry
- Status: DONE | PARTIAL (explain)
- Notes: [any spec gaps found, decisions made]
