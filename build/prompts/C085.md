# C085 — driver-ios-shell

## Identity
You are building **driver-ios-shell** for the MageRide platform.
Read `CLAUDE.md` and `apps/driver-ios/CLAUDE.md` before starting.
Wave 4b · depends on: C067 (driver-android-shell) · est. 3 session(s).

## Spec Anchors (read these files)
- `specs/D2_mageride_ui_spec.md#section-c-platform-differences`
- `specs/D2_mageride_ui_spec.md#0-2-mageride-design-tokens`
- `specs/architecture-design-document.md#18-2-mobile-architecture-kmp-native-ui`
- `specs/D7_mageride_devops.md#6-kmp-mobile-build-pipeline`
- `specs/wireframes/driver_ios.html` — the team-approved wireframe baseline for this component

## Scope
The Driver iOS application shell — the SwiftUI parity of C067. ADD: [R-07, R-17, AL-12, AL-31,
AL-43, D-30].

**Fences — do not cross these:**
- Parity-fenced to C067. Behaviour differences beyond D2 Section C platform deltas require a micro-change-set.
- This host cannot compile iOS. Generate code here; compile and verify on macOS.

## Screens (baseline = wireframes)
- None. This component is the application shell for `specs/wireframes/driver_ios.html`;
  it owns no wireframe screen ID. Screens belong to the sibling screen-group components.

## Deliverables
- apps/driver-ios Xcode project consuming the KMP XCFramework via SPM
- Color asset catalog + type scale from D2 §0.2 with Dynamic Type mapping; SF Symbols per the vehicle token table
- Localizable.strings for si / ta / en with no hardcoded user-facing strings
- NavigationStack + tab bar (Menu tab), FCM/APNs deep-link routing
- MapLibre GL Native iOS + PMTiles with the same marker/cluster/polyline patterns
- CLLocationManager background location + CocoaMQTT publish task with the KMP adaptive cadence, gps_buffer replay
- App Attest provider + Keychain/Secure Enclave device binding

## Definition of Done
- the app builds and launches on a simulator (verified on macOS) with the themed shell and tab bar
- background location + MQTT publish survive backgrounding per iOS constraints
- all copy resolves in si/ta/en with Dynamic Type respected
- App Attest tokens reach the X-Attestation header

## Verify
```
runs on macOS — xcodebuild -project apps/driver-ios/DriverApp.xcodeproj -scheme DriverApp -destination 'platform=iOS Simulator,name=iPhone 15' test
```

## Handoff
When complete, append to `build/progress.md`:
- Component: C085 driver-ios-shell
- Status: DONE | PARTIAL (explain)
- Notes: [any spec gaps found, decisions made]
