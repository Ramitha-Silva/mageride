# C105 — admin-portal-auth-dashboard

## Identity
You are building **admin-portal-auth-dashboard** for the MageRide platform.
Read `CLAUDE.md` and `portals/admin/CLAUDE.md` before starting.
Wave 4c · depends on: C104 (admin-portal-shell) · est. 1 session(s).

## Spec Anchors (read these files)
- `specs/D2_mageride_ui_spec.md#scr-ap-001-admin-login`
- `specs/D2_mageride_ui_spec.md#scr-ap-002-admin-home`
- `specs/D3_mageride_api_contracts.md#admin-bff-dashboard-statistics-filter`
- `specs/architecture-design-document.md#1-11-remediation-log-add-v2-9`
- `specs/wireframes/web_admin.html` — the team-approved wireframe baseline for this component

## Scope
Admin sign-in and the role-scoped dashboard with its statistics period filter.
ADD: [AL-37, AL-38, US-24.5, US-24.7].

**Fences — do not cross these:**
- The MFA/TOTP block is REMOVED from login — password or Google completes straight to the dashboard (AL-37).
- Period KPIs recompute on filter change; live cards (online drivers, pending verifications, open tickets) stay real-time.

## Screens (baseline = wireframes)
- SCR-AP-001 · admin_login — Login — `specs/wireframes/web_admin.html`
- SCR-AP-002 · admin_home — Role-scoped dashboard — `specs/wireframes/web_admin.html`

## Deliverables
- login page — password + Google, forgot-password, lockout messaging, no OTP/authenticator step
- dashboard — role-scoped KPI cards + alerts feed
- statistics filter — Today / This week / This month / Custom range with vs-previous-period deltas and CSV export

## Definition of Done
- every screen matches its wireframe layout, controls, states and navigation
- no MFA/authenticator UI exists on the login page
- switching to a custom range recomputes period KPIs and leaves live cards untouched
- CSV export contains exactly the filtered figures on screen
- every screen listed under `## Screens` is implemented to match the layout, controls,
  states and navigation shown in its wireframe — the wireframes are the team-approved
  baseline; any deviation requires a micro-change-set first. D2' still governs design
  tokens, styling rules and trilingual (Si/Ta/En) string resources.

## Verify
```
npm --prefix portals run lint --workspace admin && npm --prefix portals run test --workspace admin && npm --prefix portals run build --workspace admin
```

## Handoff
When complete, append to `build/progress.md`:
- Component: C105 admin-portal-auth-dashboard
- Status: DONE | PARTIAL (explain)
- Notes: [any spec gaps found, decisions made]
