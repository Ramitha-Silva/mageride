{
  "//": [
    "Local development on the slim compose stack: one gateway instance, no Redis, attestation off.",
    "None of these are safe in a deployed environment — StateStore=Memory enforces the rate limits",
    "per process, and Mode=Disabled turns D-30 off entirely."
  ],

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "MageRide.ApiGateway": "Debug"
    }
  },

  "Gateway": {
    "StateStore": "Memory",

    "ForwardedHeaders": {
      "KnownNetworks": [ "127.0.0.0/8", "::1/128" ]
    },

    "Attestation": {
      "Mode": "Disabled"
    }
  }
}
