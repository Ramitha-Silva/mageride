{
  "//": "Container 7's ONLY appsettings file. Co-located services share one content root, so a",
  "//2": "per-service appsettings.json cannot exist here — see the DropCoLocatedAppSettings target in",
  "//3": "AppServices.csproj. Everything service-specific comes from the environment, which is how",
  "//4": "infra/replica/ and infra/docker-compose.dev.yml configure this container.",

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Yarp": "Warning"
    }
  },

  "AllowedHosts": "*"
}
