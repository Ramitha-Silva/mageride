# GENERATED FILE — do not edit.
# Source: infra/k8s/service-catalog.yaml
# Regenerate: python3 infra/k8s/tools/generate_manifests.py
#
# fleet-portal  ·  portals/fleet  ·  infra/docker/Dockerfile.portal  ·  fleet.mageride.lk
#
# AL-03.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleet-portal
  labels:
    app: fleet-portal
    app.kubernetes.io/name: fleet-portal
    app.kubernetes.io/component: portal
    app.kubernetes.io/part-of: mageride
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 2
  revisionHistoryLimit: 5
  progressDeadlineSeconds: 600
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxUnavailable: 0, maxSurge: 1 }
  selector:
    matchLabels: { app: fleet-portal }
  template:
    metadata:
      labels:
        app: fleet-portal
        app.kubernetes.io/name: fleet-portal
        app.kubernetes.io/component: portal
        app.kubernetes.io/part-of: mageride
    spec:
      serviceAccountName: mageride
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels: { app: fleet-portal }
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels: { app: fleet-portal }
      terminationGracePeriodSeconds: 30
      containers:
        - name: fleet-portal
          image: ghcr.io/mageride/fleet-portal:sha-0000000
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 3002, protocol: TCP }
          envFrom:
            # NEXT_PUBLIC_* only. A portal is a browser bundle: anything it can read,
            # a user can read, so no Secret is ever mounted into one.
            - configMapRef: { name: portal-config }
          resources:
            requests: { cpu: "100m", memory: 256Mi }
            limits: { cpu: "500m", memory: 512Mi }
          # The Dockerfile's own healthcheck, as a probe: Next.js serves `/` as soon as
          # the standalone server is listening, and a redirect to /login counts (kubelet
          # treats any 2xx-3xx as success).
          readinessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 6
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
            # Next.js writes its image-optimisation cache under .next/cache at runtime.
            - { name: next-cache, mountPath: /app/portals/fleet/.next/cache }
      volumes:
        - { name: tmp, emptyDir: {} }
        - { name: next-cache, emptyDir: {} }
---
apiVersion: v1
kind: Service
metadata:
  name: fleet-portal
  labels:
    app: fleet-portal
    app.kubernetes.io/name: fleet-portal
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: mageride
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  selector: { app: fleet-portal }
  ports:
    - { name: http, port: 80, targetPort: http, protocol: TCP }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fleet-portal
  labels:
    app: fleet-portal
    app.kubernetes.io/name: fleet-portal
    app.kubernetes.io/component: autoscaling
    app.kubernetes.io/part-of: mageride
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: fleet-portal }
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
  behavior:
    # Scale up fast, down slowly. A dispatch round that lost a pod mid-offer is a
    # rider watching a spinner; a pod that lived five minutes too long is pennies.
    scaleUp: { stabilizationWindowSeconds: 30 }
    scaleDown: { stabilizationWindowSeconds: 300 }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: fleet-portal
  labels:
    app: fleet-portal
    app.kubernetes.io/name: fleet-portal
    app.kubernetes.io/component: policy
    app.kubernetes.io/part-of: mageride
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  maxUnavailable: 1
  selector:
    matchLabels: { app: fleet-portal }
