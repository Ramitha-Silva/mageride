# GENERATED FILE — do not edit.
# Source: infra/k8s/service-catalog.yaml
# Regenerate: python3 infra/k8s/tools/generate_manifests.py
#
# web-passenger  ·  portals/web-passenger  ·  infra/docker/Dockerfile.portal  ·  passenger.mageride.lk
#
# AL-44's six no-login SCR-WT pages, served in front of public-bff. Port 3003 is a
# choice, not a spec value: D7' §2.1's table predates AL-44 and lists only 3001/3002.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-passenger
  labels:
    app: web-passenger
    app.kubernetes.io/name: web-passenger
    app.kubernetes.io/component: portal
    app.kubernetes.io/part-of: mageride
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 2
  revisionHistoryLimit: 5
  progressDeadlineSeconds: 600
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxUnavailable: 0, maxSurge: 1 }
  selector:
    matchLabels: { app: web-passenger }
  template:
    metadata:
      labels:
        app: web-passenger
        app.kubernetes.io/name: web-passenger
        app.kubernetes.io/component: portal
        app.kubernetes.io/part-of: mageride
    spec:
      serviceAccountName: mageride
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels: { app: web-passenger }
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels: { app: web-passenger }
      terminationGracePeriodSeconds: 30
      containers:
        - name: web-passenger
          image: ghcr.io/ramitha-silva/web-passenger:sha-0000000
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 3003, protocol: TCP }
          envFrom:
            # No Secret is ever mounted into a portal pod. NOT because a portal is a
            # browser bundle — it is a Next SERVER that holds the session and calls the
            # gateway itself — but because it has never needed one: it authenticates
            # with the caller's own bearer, not a credential of its own.
            - configMapRef: { name: portal-config }
          resources:
            requests: { cpu: "100m", memory: 256Mi }
            limits: { cpu: "500m", memory: 512Mi }
          # The Dockerfile's own healthcheck, as a probe: Next.js serves `/` as soon as
          # the standalone server is listening, and a redirect to /login counts (kubelet
          # treats any 2xx-3xx as success).
          readinessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 6
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
            # Next.js writes its image-optimisation cache under .next/cache at runtime.
            - { name: next-cache, mountPath: /app/portals/web-passenger/.next/cache }
      volumes:
        - { name: tmp, emptyDir: {} }
        - { name: next-cache, emptyDir: {} }
---
apiVersion: v1
kind: Service
metadata:
  name: web-passenger
  labels:
    app: web-passenger
    app.kubernetes.io/name: web-passenger
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: mageride
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  selector: { app: web-passenger }
  ports:
    - { name: http, port: 80, targetPort: http, protocol: TCP }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-passenger
  labels:
    app: web-passenger
    app.kubernetes.io/name: web-passenger
    app.kubernetes.io/component: autoscaling
    app.kubernetes.io/part-of: mageride
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: web-passenger }
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
  behavior:
    # Scale up fast, down slowly. A dispatch round that lost a pod mid-offer is a
    # rider watching a spinner; a pod that lived five minutes too long is pennies.
    scaleUp: { stabilizationWindowSeconds: 30 }
    scaleDown: { stabilizationWindowSeconds: 300 }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: web-passenger
  labels:
    app: web-passenger
    app.kubernetes.io/name: web-passenger
    app.kubernetes.io/component: policy
    app.kubernetes.io/part-of: mageride
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  maxUnavailable: 1
  selector:
    matchLabels: { app: web-passenger }
