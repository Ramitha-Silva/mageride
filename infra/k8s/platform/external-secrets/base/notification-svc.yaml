# GENERATED FILE — do not edit.
# Source: infra/k8s/service-catalog.yaml
# Regenerate: python3 infra/k8s/tools/generate_manifests.py
#
# notification-svc-secret — the D7' §4.2 rows whose Secret column is yes.
#
# Vault is the only source (D7' §13). `key` is the path INSIDE the KV mount the
# ClusterSecretStore points at, and the mount is what differs per environment — so this
# file is identical in dev, staging and production and no environment name appears in it.
#
# A `key` that is not this service's own name is a SHARED credential — see `aliases` in
# service-catalog.yaml for which sentence in which options class requires it.
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: notification-svc-secret
  labels:
    app: notification-svc
    app.kubernetes.io/name: notification-svc
    app.kubernetes.io/component: secret
    app.kubernetes.io/part-of: mageride
spec:
  # 1 h, and rotation does not wait for it: the rotation procedure restarts the
  # Deployment, which re-reads the Secret ESO has already updated
  # (docs/runbooks/secret-rotation.md).
  refreshInterval: 1h
  secretStoreRef:
    name: mageride-vault
    kind: ClusterSecretStore
  target:
    name: notification-svc-secret
    creationPolicy: Owner
    # Retain: deleting the ExternalSecret must not delete the running pods' credentials.
    deletionPolicy: Retain
  data:
    - secretKey: Fcm__ServiceAccountJson
      remoteRef: { key: providers, property: fcm_service_account_json }
    - secretKey: Apns__P8Key
      remoteRef: { key: providers, property: apns_p8_key }
    - secretKey: Apns__KeyId
      remoteRef: { key: providers, property: apns_key_id }
    - secretKey: Apns__TeamId
      remoteRef: { key: providers, property: apns_team_id }
    - secretKey: Sms__NotifyLkUserId
      remoteRef: { key: providers, property: notify_lk_user_id }
    - secretKey: Sms__NotifyLkApiKey
      remoteRef: { key: providers, property: notify_lk_api_key }
    - secretKey: Sms__FitSmsApiToken
      remoteRef: { key: providers, property: fitsms_api_token }
    - secretKey: Sms__SecondaryApiKey
      remoteRef: { key: providers, property: sms_secondary_api_key }
    - secretKey: Notification__InternalApiKey
      remoteRef: { key: internal-keys, property: notification }
    - secretKey: Notification__ContentInternalApiKey
      remoteRef: { key: internal-keys, property: content }
