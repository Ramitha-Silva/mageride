# GENERATED FILE — do not edit.
# Source: infra/k8s/service-catalog.yaml
# Regenerate: python3 infra/k8s/tools/generate_manifests.py
#
# fare-svc-secret — the D7' §4.2 rows whose Secret column is yes.
#
# Vault is the only source (D7' §13). `key` is the path INSIDE the KV mount the
# ClusterSecretStore points at, and the mount is what differs per environment — so this
# file is identical in dev, staging and production and no environment name appears in it.
#
# A `key` that is not this service's own name is a SHARED credential — see `aliases` in
# service-catalog.yaml for which sentence in which options class requires it.
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fare-svc-secret
  labels:
    app: fare-svc
    app.kubernetes.io/name: fare-svc
    app.kubernetes.io/component: secret
    app.kubernetes.io/part-of: mageride
spec:
  # 1 h, and rotation does not wait for it: the rotation procedure restarts the
  # Deployment, which re-reads the Secret ESO has already updated
  # (docs/runbooks/secret-rotation.md).
  refreshInterval: 1h
  secretStoreRef:
    name: mageride-vault
    kind: ClusterSecretStore
  target:
    name: fare-svc-secret
    creationPolicy: Owner
    # Retain: deleting the ExternalSecret must not delete the running pods' credentials.
    deletionPolicy: Retain
  data:
    - secretKey: Fare__EstimateTokenKey
      remoteRef: { key: fare-svc, property: Fare__EstimateTokenKey }
    - secretKey: Onepay__ApiKey
      remoteRef: { key: providers, property: onepay_api_key }
    - secretKey: Onepay__WebhookSecret
      remoteRef: { key: providers, property: onepay_webhook_secret }
    - secretKey: LankaQr__MerchantId
      remoteRef: { key: providers, property: lankaqr_merchant_id }
    - secretKey: Fare__InternalApiKey
      remoteRef: { key: internal-keys, property: fare }
    - secretKey: Fare__DispatchInternalApiKey
      remoteRef: { key: internal-keys, property: dispatch }
    - secretKey: Fare__WalletInternalApiKey
      remoteRef: { key: internal-keys, property: wallet }
    - secretKey: Fare__RideInternalApiKey
      remoteRef: { key: internal-keys, property: ride }
