# Infra Conventions
- Docker Compose (dev + lightweight production replica), Kubernetes manifests, bash helper
  scripts. No application code lives here.
- Base images (D7' §2.1/§2.2 + Δ 2026-07-23): `aspnet:10.0-alpine` for services,
  `runtime:10.0-alpine` for the tcp-adapter workers, `node:24-alpine` for the portals
- Every service container runs as a non-root user and exposes `/health/live` + `/health/ready`
  with the D7' §5.1 healthcheck
- Env files are templates only: `.env*` is gitignored, `*.env.example` / `.env.*.example` are
  committed with placeholders. Never commit a secret — secrets are pgcrypto + Vault (D7' §13)
- Three environments, never mixed: dev compose (C009) → lightweight production replica on a
  single Contabo VPS with synthetic data, for testing/CI/demos only (C125) → production on
  DigitalOcean Kubernetes, Singapore
- Keep the replica stack DOWN during waves 0–4 — this box hosts both and ~17–20 GB of replica
  will not fit alongside a build (see the root CLAUDE.md "Build Host" note)
- MQTT (8883) is TCP passthrough at HAProxy and is never routed through the API gateway
- Verify: `bash infra/scripts/slim-verify.sh`

## Layout (C009)

```
infra/
  docker-compose.dev.slim.yml   infra only, ~5.9 GB — every component's verify uses this
  docker-compose.dev.yml        `include`s the slim file and adds the D7' §3 app containers
  env/.env.common.example       D7' §4.1 — loaded by every service
  env/.env.app.example          D7' §4.2 — loaded by app-services, hot-path, fanout,
                                tcp-adapter and api-gateway
  deploy/haproxy.cfg            edge: 443 / 8883 / 8084 / 5023-5025
  deploy/emqx/emqx.conf         JWT authenticator + listeners + D-17 rate limit
  deploy/emqx/acl.conf          D6' §3.1 topic ACL
  deploy/redpanda/bootstrap-topics.sh   D6' §2.1 topics + §2.3 DLQs
  deploy/certs/                 self-signed dev TLS, generated by dev-up.sh, gitignored
  docker/Dockerfile.service     C010 — aspnet:10.0-alpine, --build-arg SERVICE=<project dir>
  docker/Dockerfile.worker      C010 — runtime:10.0-alpine, tcp-adapter / hot-path
  docker/Dockerfile.portal      C010 — node:24-alpine, --build-arg PORTAL=<admin|fleet|web-passenger>
  scripts/dev-up.sh             [slim|full]  ·  dev-down.sh [slim|full|all] [--volumes]
  scripts/wait-healthy.sh       [compose-file] [timeout] — every container up, one-shots 0
  scripts/slim-verify.sh        the C009 definition of done, 66 checks
  scripts/migrate-verify.sh     the C003–C006 definition of done (no compose involved)
```

## Working with the dev stacks

- **One compose project, `mageride`.** Both files declare it, share the `mageride_mr`
  network and share the named volumes, so the full stack is the slim stack plus
  applications — never a second Postgres.
- **The `.example` files are the default config layer**, loaded first by every service;
  `env/.env.common` and `env/.env.app` (gitignored, absent by default) are loaded second
  and override. Nothing copies the example to the real file: a copy made once would
  shadow every later edit.
- **Compose interpolates `env_file` values.** A literal `$` in a template must be written
  `$$` or it silently becomes the empty string in the container — this bites
  `Emqx__SharedSub=$$share/posGroup/…`. `slim-verify.sh` fails on any
  "variable is not set" warning.
- **Every published port is bound to `127.0.0.1`.** This box has a public IP and also
  hosts the replica.
- **Ports are `${VAR:-default}`** so a second stack can be moved aside without editing
  the file.
- The full stack is **not buildable until Wave 2** — only `api-gateway` (C008) has a
  Dockerfile. `dev-up.sh full` says which ones are missing.

## Spec deviations that are load-bearing (all recorded in the C009 handoff)

| What D7'/the replica says | What is here, and why |
|---|---|
| `pgdata:/var/lib/postgresql/data` | `pgdata:/home/postgres/pgdata` — that is `timescale/timescaledb-ha`'s PGDATA; the spec path is the *official* postgres image's and makes first boot fail |
| `shared_preload_libraries=timescaledb,postgis-3` | `timescaledb` only — PostGIS has no preloadable library and the postmaster refuses to start |
| `redpandadata/redpanda:v24.2` | `:v24.2.26` — the bare minor tag does not exist |
| Redpanda "production mode with fsync" | `rpk redpanda mode production` before `start`; the image ships `developer_mode: true` and rpk then adds `--unsafe-bypass-fsync` |
| `EMQX_AUTHENTICATION__1__TYPE: jwt` | the key is `MECHANISM`; the whole authenticator is in `deploy/emqx/emqx.conf` and only the secret comes from the environment |
| HAProxy port `5026` (NMEA UDP) | published by the `tcp-adapter` container — HAProxy has no UDP forwarder |
